-- +goose Up
-- Руту сразу ультра. На его собственные возможности это ничего не меняет —
-- в проверках он и так вне счёта, — но пакет ему виден в «Доступах» наравне
-- со всеми, и «базовый» у того, кому открыто всё, читался бы как ошибка.
--
-- Сменить его через админку нельзя: правки пакета рута не касаются, как
-- и правки роли, доступа к играм и квоты проверок.
UPDATE users SET plan = 'ultra' WHERE role = 'root';

-- +goose Down
UPDATE users SET plan = 'basic' WHERE role = 'root';
