-- +goose Up
-- Зарегистрировавшимся сами раздел «Игры» теперь открывается сразу. Тем, кто
-- пришёл раньше, открываем задним числом. Такого узнаём по пустому created_by:
-- заведённого рутом или админом выдаёт заводящий, а у рута роль не user.
UPDATE users SET games_access = TRUE
WHERE created_by IS NULL AND role = 'user' AND NOT games_access;

-- +goose Down
-- Откатывать нечего: кому доступ открыли, а кому он был раньше, уже
-- не различить.
SELECT 1;
